include:
  - network.yml
  - services.yml
  - management.yml
  - automation.yml
  - database.yml

networks:
   cloudflare_tunnel:

volumes:
   huginn_data:
   nextcloud_data:
   wiki_images:
   wiki_data:
